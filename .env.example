# Copy this to .env.local and fill in your values.
# For local dev, point to your local MySQL instance.
# For GCP Cloud Run, use Unix socket path (see GCP_DEPLOYMENT.md).

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=cs348_movies
